# Synthetic codebook

This is invented training text, not NFPA content or a real codebook. `generate.py` recreates the
text fixture used by `make smoke`. The project intentionally commits no source PDF binary.
