# Local Docker pgvector example. Never commit a production database URL.
CODEBOOK_DATABASE_URL=postgresql://codebook:codebook-local-only@127.0.0.1:55432/codebook_test
CODEBOOK_PG_PORT=55432

# The default hash embedder is local and deterministic. Provider credentials
# are needed only when an explicitly selected provider requires them.
