# Gemini CLI entry point

Treat `AGENTS.md` as the canonical operating authority, `STATUS.md` as current
implementation truth, and `docs/CODEMAP.md` as the change map.

Begin with:

```bash
make agent-json
```

Do not commit operator content or generated derivatives. Do not perform a write
or destructive operation without its explicit apply gate.
