name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: codebook_test
          POSTGRES_USER: codebook
          POSTGRES_PASSWORD: codebook-ci-only
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U codebook -d codebook_test"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: sudo apt-get update && sudo apt-get install --yes tesseract-ocr
      - run: python -m pip install -e '.[dev,pdf,ocr,postgres]'
      - run: python -m ruff check .
      - run: python -m pytest -q
        env:
          CODEBOOK_TEST_DATABASE_URL: postgresql://codebook:codebook-ci-only@127.0.0.1:5432/codebook_test
      - run: make dry
      - run: make smoke
      - run: make leak-check
      - run: git diff --check
