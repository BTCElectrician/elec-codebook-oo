# Repository instructions

Read `AGENTS.md` before changing this repository. Use `STATUS.md` for current
implementation truth and `docs/CODEMAP.md` to find the owning contract, focused
tests, and validation command.

Run `make agent-json` for side-effect-free machine-readable orientation. Preserve
the public, synthetic-only boundary and every explicit apply gate.
